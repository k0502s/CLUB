import styled from 'styled-components';

const wrapper = styled.div`
    text-align: center;
`;
const logo = styled.img`
    width: 200px;
    height: 90px;
    margin: 15px 0 5px 0;
`;

export { logo, wrapper };
